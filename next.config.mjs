/** @type {import('next').NextConfig} */
const nextConfig = {
    allowedDevOrigins: [
      'localhost:3000',
      '127.0.0.1:3000',
      'csocaciu.ddns.net',        // your domain
    ],
};

export default nextConfig;
